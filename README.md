Bonnie's Game — Web

Local dev:

1. npm install
2. npm run dev

Build:

1. npm run build
2. npm run preview

Deploy:

- Push to GitHub and use Netlify or Vercel for free hosting. Netlify connects to GitHub and builds automatically from `npm run build`.
